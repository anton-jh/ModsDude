<Page x:Class="ModsDude.Client.Wpf.View.Pages.ProfileModsEditorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:ModsDude.Client.Wpf.View.Pages"
      xmlns:pvm="clr-namespace:ModsDude.Client.Wpf.ViewModel.Pages"
      xmlns:vm="clr-namespace:ModsDude.Client.Wpf.ViewModel.ViewModels"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProfileModsEditorPage"
      d:DataContext="{d:DesignInstance Type=pvm:ProfileModsEditorPageViewModel, IsDesignTimeCreatable=False}">

    <Grid RowDefinitions="*, auto">

        <Border CornerRadius="4 0 0 4" Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}" Padding="32">
            <Grid RowDefinitions="auto auto auto *">

                <TextBlock Grid.Row="0"
                           Style="{DynamicResource TitleTextBlockStyle}" Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                           Text="{Binding ProfileName}" />
                <TextBlock Grid.Row="1"
                           Style="{DynamicResource SubtitleTextBlockStyle}" Foreground="{DynamicResource AccentTextFillColorSecondaryBrush}"
                           Text="Mods"
                           Margin="0 8 0 24" />

                <!-- Kept until something changes again, so the result of a save is still on screen
                     to read. -->
                <Border Grid.Row="2"
                        Margin="0 0 0 12"
                        Padding="12 8"
                        CornerRadius="4"
                        Background="{DynamicResource ControlFillColorSecondaryBrush}"
                        Visibility="{Binding HasSaveSummary, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="{Binding SaveSummary}"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                </Border>

                <Grid Grid.Row="3" ColumnDefinitions="* 16 *">

                    <!-- The available side: registered mods and local candidates in one list, so a
                         mod can be added and imported without a detour to the management page. -->
                    <Grid Grid.Column="0" RowDefinitions="auto * auto">

                        <Grid Grid.Row="0" Margin="0 0 0 8" ColumnDefinitions="* auto">

                            <Grid Grid.Column="0">
                                <!-- Right padding keeps the text from running under the clear
                                     button. The rest matches the theme's own TextBox padding. -->
                                <TextBox x:Name="SearchBox"
                                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=150}"
                                         Padding="10 5 32 6"
                                         VerticalAlignment="Center" />

                                <!-- Watches the box rather than the view model: the binding above
                                     delays pushing the text through, so anything driven off
                                     SearchText stays a beat behind what has actually been typed. -->
                                <TextBlock Text="Search by name, id or author"
                                           Margin="12 0 0 0"
                                           VerticalAlignment="Center"
                                           IsHitTestVisible="False"
                                           Foreground="{DynamicResource TextFillColorTertiaryBrush}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>

                                <Button Width="24" Height="24"
                                        FontSize="12"
                                        Margin="0 0 4 0"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Center"
                                        Content="&#xE10A;"
                                        ToolTip="Clear search"
                                        Command="{Binding ClearSearchCommand}">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource SubtleIconButton}">
                                            <Setter Property="Visibility" Value="Visible" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </Grid>

                            <TextBlock Grid.Column="1"
                                       Margin="12 0 0 0"
                                       VerticalAlignment="Center"
                                       Text="{Binding AvailableCountText}"
                                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        </Grid>

                        <Border Grid.Row="1"
                                CornerRadius="6"
                                BorderThickness="1"
                                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                                Background="{DynamicResource SolidBackgroundFillColorBaseBrush}">
                            <Grid>

                                <!-- Virtualized with recycling: a mod folder can easily hold a few
                                     thousand archives. -->
                                <ItemsControl ItemsSource="{Binding AvailableView}"
                                              VirtualizingStackPanel.IsVirtualizing="True"
                                              VirtualizingStackPanel.VirtualizationMode="Recycling"
                                              ScrollViewer.CanContentScroll="True">
                                    <ItemsControl.Template>
                                        <ControlTemplate TargetType="ItemsControl">
                                            <ScrollViewer CanContentScroll="True"
                                                          Focusable="False"
                                                          VerticalScrollBarVisibility="Auto"
                                                          HorizontalScrollBarVisibility="Disabled">
                                                <ItemsPresenter />
                                            </ScrollViewer>
                                        </ControlTemplate>
                                    </ItemsControl.Template>
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <VirtualizingStackPanel />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>

                                    <!-- The shared row, plus the one thing this list adds to it. The
                                         inner presenter picks up the implicit template, so a mod
                                         looks the same here as it does everywhere else. -->
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="{x:Type vm:ModListItemViewModel}">
                                            <Grid ColumnDefinitions="* auto">
                                                <ContentPresenter Grid.Column="0" Content="{Binding}" />

                                                <Button Grid.Column="1"
                                                        Margin="0 0 8 0"
                                                        VerticalAlignment="Center"
                                                        Style="{DynamicResource SubtleIconButton}"
                                                        Content="&#xE710;"
                                                        ToolTip="Add to this profile"
                                                        Command="{Binding DataContext.AddCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                        CommandParameter="{Binding}" />
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <TextBlock Text="Reading the repo and scanning the sources…"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                           Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}" />
                            </Grid>
                        </Border>

                        <!-- The sources feed this list and nothing else, so they sit under it.
                             Disabling an instance here says nothing about syncing to it. -->
                        <Expander Grid.Row="2" Margin="0 8 0 0" Header="Sources">
                            <Grid RowDefinitions="auto auto" Margin="0 8 0 0">

                                <Border Grid.Row="0"
                                        MaxHeight="200"
                                        CornerRadius="6"
                                        BorderThickness="1"
                                        BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                                        Background="{DynamicResource SolidBackgroundFillColorBaseBrush}">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="4">
                                        <ItemsControl ItemsSource="{Binding Sources}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate DataType="{x:Type vm:ModSourceViewModel}">
                                                    <Grid Margin="6 6" ColumnDefinitions="auto * auto">

                                                        <CheckBox Grid.Column="0"
                                                                  VerticalAlignment="Top"
                                                                  Margin="0 2 0 0"
                                                                  IsChecked="{Binding IsEnabled, Mode=TwoWay}" />

                                                        <StackPanel Grid.Column="1" ToolTip="{Binding Path}">
                                                            <TextBlock Text="{Binding Name}"
                                                                       TextTrimming="CharacterEllipsis"
                                                                       TextWrapping="NoWrap"
                                                                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" />

                                                            <!-- One unreadable folder marks one
                                                                 source bad rather than failing the
                                                                 whole list. -->
                                                            <TextBlock Text="{Binding CountText}"
                                                                       ToolTip="{Binding Error}">
                                                                <TextBlock.Style>
                                                                    <Style TargetType="TextBlock">
                                                                        <Setter Property="FontSize" Value="12" />
                                                                        <Setter Property="Foreground" Value="{DynamicResource TextFillColorTertiaryBrush}" />
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding HasFailed}" Value="True">
                                                                                <Setter Property="Foreground" Value="{DynamicResource SystemFillColorCriticalBrush}" />
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </TextBlock.Style>
                                                            </TextBlock>
                                                        </StackPanel>

                                                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Top">
                                                            <Button Style="{DynamicResource SubtleIconButton}"
                                                                    Width="28"
                                                                    Content="&#xE72C;"
                                                                    ToolTip="Rescan this source"
                                                                    Command="{Binding DataContext.RescanSourceCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                                    CommandParameter="{Binding}" />
                                                            <Button Style="{DynamicResource SubtleIconButton}"
                                                                    Width="28"
                                                                    Content="&#xE711;"
                                                                    ToolTip="Remove this folder"
                                                                    Visibility="{Binding IsAdHoc, Converter={StaticResource BoolToVisibilityConverter}}"
                                                                    Command="{Binding DataContext.RemoveSourceCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                                    CommandParameter="{Binding}" />
                                                        </StackPanel>
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </Border>

                                <Grid Grid.Row="1" Margin="0 8 0 0" ColumnDefinitions="* auto">
                                    <Button Grid.Column="0"
                                            HorizontalAlignment="Stretch"
                                            Content="Add a folder…"
                                            ToolTip="Scanned for this session only, and never written to disk"
                                            Command="{Binding AddSourceCommand}" />
                                    <Button Grid.Column="1"
                                            Margin="8 0 0 0"
                                            Style="{DynamicResource SubtleIconButton}"
                                            Content="&#xE72C;"
                                            ToolTip="Rescan every source"
                                            Command="{Binding RescanAllCommand}" />
                                </Grid>
                            </Grid>
                        </Expander>
                    </Grid>

                    <!-- The profile itself. Updates live here rather than on the left, so a mod with
                         a newer version is never on both sides at once. -->
                    <Grid Grid.Column="2" RowDefinitions="auto auto *">

                        <Grid Grid.Row="0" Margin="0 0 0 8" ColumnDefinitions="* auto">
                            <TextBlock Grid.Column="0"
                                       Text="In this profile"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                       Style="{DynamicResource BodyStrongTextBlockStyle}" />
                            <TextBlock Grid.Column="1"
                                       VerticalAlignment="Center"
                                       Text="{Binding PinnedCountText}"
                                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        </Grid>

                        <!-- The batch action applies to unlocked mods only, and says how many it
                             left alone. The skipped count is a link to the same dialog the per-row
                             change opens, reached deliberately rather than fired at every save. -->
                        <Border Grid.Row="1"
                                Margin="0 0 0 8"
                                Padding="12 8"
                                CornerRadius="4"
                                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                                Visibility="{Binding HasUpdates, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Grid ColumnDefinitions="auto * auto">
                                <TextBlock Grid.Column="0"
                                           VerticalAlignment="Center"
                                           Text="{Binding UpdateCountText}"
                                           Foreground="{DynamicResource TextFillColorPrimaryBrush}" />

                                <Button Grid.Column="1"
                                        Margin="12 0 0 0"
                                        Padding="6 2"
                                        HorizontalAlignment="Left"
                                        Style="{DynamicResource SubtleMenuButton}"
                                        Foreground="{DynamicResource SystemFillColorCautionBrush}"
                                        Content="{Binding SkippedText}"
                                        ToolTip="Locked mods are skipped. Open the list to move any of them anyway."
                                        Visibility="{Binding HasSkippedUpdates, Converter={StaticResource BoolToVisibilityConverter}}"
                                        Command="{Binding ShowSkippedUpdatesCommand}" />

                                <Button Grid.Column="2"
                                        Content="{Binding ApplyUpdatesText}"
                                        Command="{Binding ApplyAllUpdatesCommand}" />
                            </Grid>
                        </Border>

                        <Border Grid.Row="2"
                                CornerRadius="6"
                                BorderThickness="1"
                                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                                Background="{DynamicResource SolidBackgroundFillColorBaseBrush}">
                            <Grid>

                                <ItemsControl ItemsSource="{Binding PinnedView}"
                                              VirtualizingStackPanel.IsVirtualizing="True"
                                              VirtualizingStackPanel.VirtualizationMode="Recycling"
                                              ScrollViewer.CanContentScroll="True">
                                    <ItemsControl.Template>
                                        <ControlTemplate TargetType="ItemsControl">
                                            <ScrollViewer CanContentScroll="True"
                                                          Focusable="False"
                                                          VerticalScrollBarVisibility="Auto"
                                                          HorizontalScrollBarVisibility="Disabled">
                                                <ItemsPresenter />
                                            </ScrollViewer>
                                        </ControlTemplate>
                                    </ItemsControl.Template>
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <VirtualizingStackPanel />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>

                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="{x:Type vm:ProfileModRowViewModel}">
                                            <Grid ColumnDefinitions="* auto">

                                                <ContentPresenter Grid.Column="0" Content="{Binding Item}" />

                                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">

                                                    <!-- Nothing has been uploaded for this row yet;
                                                         Save is what imports it, and Cancel is what
                                                         discards it. -->
                                                    <Border Margin="0 0 8 0"
                                                            Padding="8 2"
                                                            VerticalAlignment="Center"
                                                            CornerRadius="3"
                                                            ToolTip="Not in the repo yet. Saving uploads and registers it first."
                                                            Background="{DynamicResource SystemFillColorAttentionBrush}"
                                                            Visibility="{Binding IsPending, Converter={StaticResource BoolToVisibilityConverter}}">
                                                        <TextBlock Text="Pending"
                                                                   Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                                                   Style="{DynamicResource CaptionTextBlockStyle}" />
                                                    </Border>

                                                    <Button Style="{DynamicResource SubtleIconButton}"
                                                            Content="&#xE777;"
                                                            ToolTip="{Binding UpdateTooltip}"
                                                            Visibility="{Binding HasUpdate, Converter={StaticResource BoolToVisibilityConverter}}"
                                                            Command="{Binding DataContext.UpdateOneCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                            CommandParameter="{Binding}" />

                                                    <!-- The list is keyed by mod, so which version
                                                         it holds is a choice on the row. -->
                                                    <ComboBox Width="150"
                                                              Margin="4 0"
                                                              VerticalAlignment="Center"
                                                              ItemsSource="{Binding Versions}"
                                                              SelectedItem="{Binding SelectedVersion, Mode=TwoWay}"
                                                              DisplayMemberPath="Label"
                                                              IsEnabled="{Binding HasSeveralVersions}"
                                                              ToolTip="Which version this profile pins" />

                                                    <ToggleButton Style="{DynamicResource SubtleIconToggleButton}"
                                                                  Content="&#xE72E;"
                                                                  ToolTip="{Binding LockTooltip}"
                                                                  IsChecked="{Binding LockedByProfile, Mode=TwoWay}" />

                                                    <Button Margin="0 0 8 0"
                                                            Style="{DynamicResource SubtleIconButton}"
                                                            Content="&#xE738;"
                                                            ToolTip="Take out of this profile"
                                                            Command="{Binding DataContext.RemoveCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                            CommandParameter="{Binding}" />
                                                </StackPanel>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <TextBlock Text="Nothing in this profile yet.&#x0a;Add mods from the list on the left."
                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                           TextAlignment="Center"
                                           Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding IsLoading}" Value="False" />
                                                        <Condition Binding="{Binding HasPinnedMods}" Value="False" />
                                                    </MultiDataTrigger.Conditions>
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </MultiDataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>
            </Grid>
        </Border>

        <Grid Grid.Row="1" ColumnDefinitions="auto auto * auto">

            <Button Grid.Column="0" Width="250" Margin="32 24 4 24" Style="{DynamicResource AccentButtonStyle}"
                    Content="Save changes" Command="{Binding SaveChangesCommand}" />

            <Button Grid.Column="1" Margin="4 24 4 24"
                    Content="Discard changes" Command="{Binding DiscardChangesCommand}" />

            <TextBlock Grid.Column="2"
                       Margin="16 0"
                       VerticalAlignment="Center"
                       Text="{Binding PendingText}"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       Visibility="{Binding HasPending, Converter={StaticResource BoolToVisibilityConverter}}" />

            <Button Grid.Column="3" Margin="4 24 32 24"
                    Content="Stop saving"
                    Command="{Binding SaveChangesCancelCommand}"
                    Visibility="{Binding IsSaving, Converter={StaticResource BoolToVisibilityConverter}}" />
        </Grid>

    </Grid>

</Page>
