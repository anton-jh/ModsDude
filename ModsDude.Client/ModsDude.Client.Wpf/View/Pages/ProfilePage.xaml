<Page x:Class="ModsDude.Client.Wpf.View.Pages.ProfilePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:pvm="clr-namespace:ModsDude.Client.Wpf.ViewModel.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      d:DataContext="{d:DesignInstance Type=pvm:DesignTimeProfilePageViewModel, IsDesignTimeCreatable=False}"
      Title="ProfilePage">

    <Border CornerRadius="4 0 0 0" Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}" Padding="32">
        <StackPanel Orientation="Vertical" >

            <TextBlock Style="{DynamicResource TitleTextBlockStyle}" Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                   Margin="0 0 0 24"
                   Text="{Binding Name}" />

        </StackPanel>
    </Border>
</Page>
