<UserControl x:Class="ModsDude.Client.Wpf.View.UserControls.PageTitle"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <!-- What the page is, and nothing about where it is: the repo and the profile are named in the
         header above, and repeating them here is what this control exists to stop. The gap below is
         part of it rather than something every page sets for itself, so no two pages disagree. -->
    <TextBlock Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}}"
               Margin="0 0 0 24"
               Style="{DynamicResource TitleTextBlockStyle}"
               Foreground="{DynamicResource TextFillColorPrimaryBrush}"
               TextTrimming="CharacterEllipsis" />
</UserControl>
